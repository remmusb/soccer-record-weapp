# 野球记录小程序 - 正式测试前自检清单

## 一、上线前配置（必须先完成）

### 1. 云函数部署（微信开发者工具）
在开发者工具中，右键每个云函数目录，选择**"创建并部署：云端安装依赖"**：

| 顺序 | 云函数 | 说明 |
|------|--------|------|
| 1 | `addAdmin` | 添加管理员 |
| 2 | `cancelRegistration` | 取消报名 |
| 3 | `createPlayer` | 创建球员（管理员） |
| 4 | `deletePlayer` | 删除球员 |
| 5 | `exportData` | 导出数据 |
| 6 | `getAdmins` | 获取管理员列表 |
| 7 | `getPlayerMatches` | 获取球员场主/护法比赛 |
| 8 | `getPlayers` | 获取所有球员 |
| 9 | `login` | 登录获取身份 |
| 10 | `processRegistration` | 确认名单排序 |
| 11 | `recalculateStats` | 重新计算统计 |
| 12 | `removeAdmin` | 移除管理员 |
| 13 | `sendNotification` | 发送通知 |
| 14 | `sendReminders` | 定时赛前提醒 |

### 2. 数据库配置（云开发控制台）
创建以下集合（如果还没有）：
- `players` - 球员资料
- `matches` - 比赛场次
- `admins` - 管理员列表
- `settings` - 系统设置

### 3. 管理员初始化
在数据库 `admins` 集合中手动添加第一条记录：
```json
{
  "playerId": "你的playerId",
  "createdAt": { "$date": "当前时间" }
}
```

### 4. 订阅消息模板ID（可选）
代码中所有 `YOUR_TEMPLATE_ID_A` 和 `YOUR_TEMPLATE_ID_C` 是占位符，需要替换为真实模板ID：
- 登录微信公众平台 → 功能 → 订阅消息 → 申请模板
- 替换 `sendNotification/index.js` 和 `sendReminders/index.js` 中的模板ID

---

## 二、个人测试Checklist（按顺序）

### 阶段1：基础功能（必测）

- [ ] **1.1 创建球员资料**
  - 为自己创建球员（头像/姓名/昵称/位置/初始分）
  - 验证头像显示正确
  - 验证综合评分按算法计算（初始分7 → 综合6.6）

- [ ] **1.2 管理员创建其他球员**
  - 添加7-8个测试球员
  - 验证头像显示
  - 验证系统评分显示正确

- [ ] **1.3 设置管理员**
  - 进入设置页 → 添加至少1个管理员
  - 验证管理员列表显示

- [ ] **1.4 创建比赛**
  - 选择日期 → 验证默认结束时间 = 开始时间+1.5h
  - 地图选点（如不可用则手动输入）
  - 验证截图截止时间默认 = 比赛日前13天24点
  - 手动修改截图截止时间 → 验证保存正确
  - 创建需抽场和无需抽场各一场

- [ ] **1.5 报名功能**
  - 普通球员自己报名 → 验证状态正常
  - 管理员为其他球员报名
  - 截图截止时间前报名 → 状态"pending_screenshot"，提示上传截图
  - 截图截止时间后报名 → 状态"confirmed"，直接确认
  - 超过maxPlayers后报名 → 状态"WL"（候补）

- [ ] **1.6 上传截图**
  - 点击"上传截图" → 选择图片 → 上传成功
  - 验证状态变为"confirmed"
  - 点击"查看截图" → 正常显示

- [ ] **1.7 管理员操作**
  - 设定场主 → 验证显示👑
  - 添加护法（最多4人）→ 验证显示🛡️
  - 添加第5个护法 → 提示"护法最多4人"
  - 移除护法 → 验证从列表消失
  - 更换场主 → 验证更换成功
  - 关闭/开启报名 → 验证按钮切换
  - 确认名单 → 验证场主/护法优先级最高，截图优先，按报名时间
  - 重新确认名单 → 验证可以重新排序
  - 删除场次 → 验证删除成功
  - 编辑场次 → 验证信息更新

- [ ] **1.8 开始比赛**
  - 点击"开始比赛" → 状态变为"ongoing"
  - 验证"记录赛况"按钮出现

- [ ] **1.9 记录赛况**
  - 进入记录赛况 → 添加比分
  - 添加进球/助攻/黄牌/红牌等事件
  - 保存 → 验证成功

- [ ] **1.10 结束比赛**
  - 点击"结束比赛" → 状态变为"completed"
  - 验证统计自动更新

- [ ] **1.11 赛后评分**
  - 结束时间后24小时内 → 评分按钮出现
  - 队友互评 → 验证24小时有效
  - 管理员评分 → 验证正常
  - 提交 → 验证综合评分更新

- [ ] **1.12 重新开启比赛**
  - 已结束比赛 → 点击"重新开启" → 状态变回"upcoming"

### 阶段2：分队功能（必测）

- [ ] **2.1 自动分队**
  - 确认名单后 → 进入自动分队
  - 验证分队均衡（人数、评分、年龄）
  - 验证位置均衡（门将各1，后场:中场:前锋 ≈ 2:2:1）
  - 验证阵型图显示正确（绿色足球场背景）
  - 设定队服颜色 → 对阵信息中显示颜色
  - 设定队长 → 验证保存

- [ ] **2.2 手动调整分队**
  - 移动球员从A到B → 验证阵型图实时更新
  - 添加队服颜色 → 保存后验证显示

### 阶段3：统计功能（必测）

- [ ] **3.1 球员列表**
  - 验证头像显示
  - 验证综合评分排序
  - 验证场主/护法次数标签

- [ ] **3.2 球员详情**
  - 验证出场/进球/助攻/胜平负统计
  - 切换全部/本年度/本月 → 数据变化正确
  - 验证场主/护法比赛列表
  - 验证评分历史

- [ ] **3.3 设置页**
  - 点击"重新计算统计" → 验证所有球员评分更新

### 阶段4：边界情况（必测）

- [ ] **4.1 取消报名**
  - 比赛前24小时以上取消 → 成功
  - 比赛前24小时内取消 → 提示不可取消

- [ ] **4.2 未创建球员的用户**
  - 打开小程序 → 提示创建球员
  - 无法报名 → 提示先创建资料

- [ ] **4.3 非管理员**
  - 看不到创建场次按钮
  - 看不到管理员操作区
  - 只能为自己报名

- [ ] **4.4 旧数据兼容**
  - 打开之前创建的场次 → 不白屏
  - 旧数据中的`assistantId`自动转为`assistantIds`数组

### 阶段5：通知功能（可选）

- [ ] **5.1 订阅消息**
  - 首次报名触发时 → 弹出订阅授权
  - 名单确认后 → 已确认球员收到通知
  - 候补晋升时 → 候补球员收到通知
  - 场主/护法设定时 → 相关人员收到通知

---

## 三、测试人员分工（8-9人）

| 角色 | 人数 | 微信号 | 测试重点 |
|------|------|--------|----------|
| 你（管理员+场主） | 1 | 你的微信 | 全部管理功能，创建球员，创建场次，确认名单，分队，评分 |
| 管理员B | 1 | 朋友A | 验证管理员权限，为他人报名，删除场次 |
| 普通球员A（护法） | 1 | 朋友B | 自己报名，上传截图，分队后评分 |
| 普通球员B（候补） | 1 | 朋友C | 报名后进入候补，验证候补晋升 |
| 普通球员C | 1 | 朋友D | 不截图报名，验证pending_screenshot状态 |
| 普通球员D | 1 | 朋友E | 评分功能，24小时窗口验证 |
| 未创建球员用户 | 1 | 朋友F | 首次进入流程，创建球员引导 |
| 凑人数球员 | 3 | 朋友G/H/I | 凑到maxPlayers以上，测试候补机制 |

---

## 四、常见问题排查

| 问题 | 排查方法 |
|------|----------|
| 云函数报错"Cannot find module" | 检查package.json是否存在，重新部署"云端安装依赖" |
| 数据库查不到数据 | 检查数据库权限是否设为"所有用户可读，仅创建者可写" |
| 报名后白屏 | 检查`endTime`格式兼容性，查看控制台报错 |
| 头像不显示 | 检查云存储fileID是否正确，检查avatar字段是否保存 |
| 评分不更新 | 检查`recalculateStats`云函数日志，检查是否有`?.`语法错误 |
| 无法获取球员列表 | 检查`getPlayers`云函数是否部署，检查数据库权限 |

---

## 五、部署到体验版步骤

1. **微信开发者工具** → 右上角"上传" → 填写版本号（如1.0.5）
2. **登录微信公众平台** → 版本管理 → 开发版本 → 设为体验版
3. **添加体验成员**：微信公众平台 → 管理 → 成员管理 → 体验成员 → 添加微信号（最多100人）
4. **扫码测试**：用体验成员的微信扫描体验版二维码
5. **收集反馈**：让每个人截图问题，汇总后修复

---

## 六、已知待办事项

| 优先级 | 事项 | 说明 |
|--------|------|------|
| 🔴 高 | 模板ID替换 | 所有`YOUR_TEMPLATE_ID_A/C`需替换为真实模板ID，否则通知不可用 |
| 🟡 中 | 地图接口开通 | 微信公众平台 → 开发 → 开发管理 → 接口设置 → 开通地理位置 |
| 🟢 低 | 定时触发器 | `sendReminders`需要在腾讯云控制台配置定时触发（每小时） |
| 🟢 低 | 小程序改名 | 从"欢足报名统计"改为新名字（如"欢期"），需审核 |

---

测试完成后，把发现的问题列表发给我，我逐个修复。
