# 真机测试环境搭建指南（多角色测试）

## 核心问题

微信小程序的 `openid` 是**微信账号唯一绑定**的，一个人无法同时拥有多个 openid。要测试不同角色（管理员、普通队员、场主等），你需要**多个真实微信账号**参与。

---

## 方案一：找3-5个队友帮忙（最推荐）

### 准备人员

| 角色 | 需要几个人 | 做什么 |
|------|-----------|------|
| **管理员** | 1人 | 你的微信账号，已在后台设为管理员 |
| **普通队员A** | 1人 | 创建球员资料，报名比赛，上传截图 |
| **普通队员B** | 1人 | 报名但不传截图，进入候补状态 |
| **场主/护法** | 1-2人 | 被管理员设为场主/护法 |
| **替补队员** | 1-2人 | 候补角色，测试候补晋升 |

### 操作步骤

1. **管理员（你）** 先创建小程序，部署云函数，添加管理员
2. **发给队友**：把微信开发者工具的预览二维码截图发到微信群
3. **队友扫码**：用微信扫码进入小程序预览版
4. **队友创建球员**：每个人在"我的"页创建自己的球员资料
5. **管理员添加队员为管理员**（可选，如果需要测试管理员功能）

> ⚠️ **预览二维码有效期15分钟**，过期需要重新生成。正式发布后可以用正式版测试。

---

## 方案二：一个人测试（用"体验版"+切换账号）

如果你找不到队友，可以**用两个微信号**（自己的微信 + 家人的微信/小号）测试。

### 步骤

1. **把你的微信设为管理员**
   - 进入小程序 → 设置页 → 添加管理员 → 选择自己

2. **用另一个微信号扫码进入小程序**
   - 生成预览二维码
   - 用另一个手机/微信扫码
   - 在另一个手机上创建球员资料

3. **记录两个 openid**
   - 在小程序某个页面添加调试按钮，点击调用 `login` 云函数，显示 openid
   - 或者查看云开发控制台 → 数据库 → `players` 集合，看 `_openid` 字段

4. **在数据库中手动设置管理员**
   - 云开发控制台 → 数据库 → `admins` 集合
   - 添加一条记录：`{ playerId: "另一个球员的_id", createdAt: 时间 }`

5. **切换测试**
   - 测试管理员功能：用你的微信（管理员）操作
   - 测试普通队员：用另一个微信操作

---

## 方案三：云函数模拟不同用户（高级）

如果找不到队友，可以**通过云函数/数据库手动模拟**不同角色的数据，跳过前端操作。

### 模拟场景：测试报名和候补

1. 管理员创建一场比赛
2. 在云开发控制台 → 数据库 → `matches` 集合，找到该比赛
3. 直接编辑 `registrations` 字段，手动添加多个报名记录：

```json
{
  "registrations": [
    { "playerId": "playerA_id", "status": "pending_screenshot", "registeredAt": "2025-06-15T10:00:00Z" },
    { "playerId": "playerB_id", "status": "confirmed", "screenshot": "cloud://...", "registeredAt": "2025-06-15T10:05:00Z" },
    { "playerId": "playerC_id", "status": "confirmed", "screenshot": "cloud://...", "registeredAt": "2025-06-15T10:10:00Z" },
    { "playerId": "playerD_id", "status": "confirmed", "registeredAt": "2025-06-15T10:15:00Z" },
    { "playerId": "playerE_id", "status": "WL", "registeredAt": "2025-06-15T10:20:00Z" }
  ]
}
```

4. 刷新小程序页面，看报名列表是否正确显示
5. 测试"确认名单"功能，看排序和WL状态是否正确

### 模拟场景：测试场主/护法

1. 在数据库中直接设置 `ownerId` 和 `assistantId`：

```json
{
  "ownerId": "playerA_id",
  "assistantId": "playerB_id"
}
```

2. 刷新页面，看场主/护法是否正确显示

### 模拟场景：测试分队

1. 在数据库中直接设置分队：

```json
{
  "teamA": {
    "players": ["playerA_id", "playerC_id"],
    "score": 0,
    "color": "红色",
    "captainId": "playerA_id",
    "name": "A队"
  },
  "teamB": {
    "players": ["playerB_id", "playerD_id"],
    "score": 0,
    "color": "蓝色",
    "captainId": "playerB_id",
    "name": "B队"
  }
}
```

2. 刷新页面，看分队是否正确显示

---

## 方案四：使用"体验版"代替预览（更稳定）

预览版二维码15分钟过期，体验版可以长期访问。

### 步骤

1. 在微信开发者工具中，点击右上角"**上传**"按钮
2. 填写版本号（如 1.0.0）和项目备注
3. 进入微信公众平台 → 版本管理 → 开发版本
4. 点击"**选为体验版**"
5. 生成体验版二维码，可以长期有效
6. 把二维码发给队友，扫码进入即可

> 体验版需要**添加体验成员**：微信公众平台 → 成员管理 → 体验成员 → 添加队友的微信号。

---

## 快速获取用户 openid

在小程序任意页面添加一个调试按钮，点击显示 openid：

```javascript
// 在某个页面临时添加
async showOpenid() {
  const { result } = await wx.cloud.callFunction({ name: 'login' });
  uni.showModal({
    title: '调试信息',
    content: `openid: ${result.openid}\nplayerId: ${result.playerId || '无'}\nisAdmin: ${result.isAdmin}`,
    showCancel: false
  });
}
```

或者在云开发控制台查看：
- 云开发控制台 → 数据库 → `players` 集合 → `_openid` 字段

---

## 数据库快速清理/重置

测试过程中需要反复清理数据，可以在云开发控制台：

1. **删除所有比赛**：`matches` 集合 → 全选 → 删除
2. **删除所有报名记录**：同上
3. **重置球员统计**：`players` 集合 → 编辑 → 把 `stats` 字段重置为初始值

---

## 推荐测试流程（找2个队友）

| 步骤 | 谁操作 | 做什么 |
|------|--------|--------|
| 1 | 管理员 | 创建比赛，需抽场，人数上限4 |
| 2 | 队员A | 扫码进入，创建球员，报名，上传截图 |
| 3 | 队员B | 扫码进入，创建球员，报名（不上传截图） |
| 4 | 队员C | 扫码进入，创建球员，报名（不上传截图） |
| 5 | 管理员 | 查看报名列表，确认名单（A确认，B/C/D按顺序，E为WL） |
| 6 | 管理员 | 设定场主/护法 |
| 7 | 管理员 | 分队，保存 |
| 8 | 队员A | 退出报名，触发候补晋升 |
| 9 | 候补队员 | 收到晋升通知，检查状态变为确认 |
| 10 | 所有人 | 检查收到的微信服务通知 |

---

## 常见问题

### Q1：预览二维码扫不进去？
- 检查是否已添加体验成员（微信公众平台 → 成员管理）
- 检查小程序是否已上传代码
- 二维码有效期15分钟，过期重新生成

### Q2：另一个微信号创建不了球员？
- 检查数据库权限：`_openid` 是系统字段，不能手动写入
- 确保用 `createPlayer` 云函数创建，而不是客户端直接 `add`

### Q3：通知发送失败？
- 检查模板ID是否已替换为真实ID
- 检查用户是否授权了订阅消息（首次需要弹窗授权）
- 在云函数日志中查看具体错误码

### Q4：如何快速切换管理员身份测试？
- 在数据库 `admins` 集合中，手动添加/删除记录来控制谁是管理员
- 或者在 `login` 云函数中临时硬编码 `isAdmin: true` 来测试

---

## 最简测试方案（1人+数据库操作）

如果完全找不到队友，用最简方案：

1. 你自己创建1个球员资料
2. 管理员创建比赛
3. 在数据库 `matches` 中手动添加多条 `registrations` 记录（模拟多个队员报名）
4. 测试所有功能：确认名单、设场主、分队、候补晋升
5. 通知功能用 `sendNotification` 云函数的测试按钮手动触发

这个方案可以测试80%的功能，唯独不能测试"另一个真实微信收到通知"的效果（需要真实用户授权）。
