# 部署与测试指南：头像/昵称/截图修复

## 本次修复的三个问题

1. **用户注册时选择微信头像和昵称不能成功填入**
2. **用户的抽场截图点开后是空白（只能看到自己的截图）**
3. **用户可以修改自己的球员信息，包括头像、位置、是否公开评分等注册时所有信息**

---

## 一、部署步骤

### 步骤 1：重新编译上传 `pages/players/create.vue`

在 HBuilderX 中：
1. 找到 `pages/players/create.vue`
2. 右键 → **重新编译**（或直接保存触发自动编译）
3. 上传流程：
   - 工具栏点击 **运行 → 运行到小程序模拟器 → 微信开发者工具**
   - 在微信开发者工具中：点击 **上传** → 填写版本号（如 `1.2.3`）→ 确定

> 注：`detail.vue` 的截图查看修复已在之前的版本中部署，本次确认即可。

### 步骤 2：部署 `registerMatch` 云函数（如果尚未部署）

1. 打开微信开发者工具
2. 展开 **cloudfunctions/registerMatch/**
3. 右键 `registerMatch` 文件夹 → **创建并部署：云端安装依赖**
4. 等待部署完成

> 如果已部署过，可以跳过此步骤。

### 步骤 3：提交审核（如果是正式版）

1. 登录[微信公众平台](https://mp.weixin.qq.com)
2. 进入 **版本管理**
3. 找到刚刚上传的版本 → **提交审核**
4. 审核通过后 → **发布**

---

## 二、测试验证

### 测试 1：微信头像和昵称获取（create.vue）

**场景 A：新用户注册**

1. 使用一个**未注册过的新微信账号**（或清除缓存后）进入小程序
2. 进入 **"我的" → "注册成为球员"**
3. 点击 **头像区域** → 选择 **"使用微信头像"**
   - 应弹出微信的头像选择界面
   - 选择一个头像后，头像应显示在表单中
4. 点击 **昵称输入框** → 应看到键盘上方出现 **"使用微信昵称"** 的提示
   - 点击该提示，昵称应自动填入
   - 或手动输入昵称
5. 填写其他必填项（姓名、位置等）
6. 点击 **提交**
7. 验证：提交成功后，进入 "球员名单"，应能看到新注册球员的头像和昵称

**场景 B：已有用户修改信息**

1. 使用已注册的账号进入小程序
2. 进入 **"我的" → "注册成为球员"**（此时显示为编辑模式）
3. 应能看到当前已设置的头像、昵称、出生年月、评分权限等信息
4. 点击 **头像区域** → 选择新头像（从相册或微信头像）
5. 修改昵称、出生年月、评分权限等任意字段
6. 点击 **保存修改**
7. 验证：保存成功后，重新进入该页面，所有修改应已保留

**预期问题排查**：
- 如果 "使用微信头像" 无反应：检查微信基础库版本 ≥ 2.21.2
- 如果昵称无法自动填入：确认输入框使用了 `type="nickname"`

---

### 测试 2：截图查看（detail.vue）

**场景：普通用户查看其他用户的截图**

1. 管理员创建一个需要截图报名的比赛
2. 用户 A（非管理员）报名并上传截图
3. 用户 B（非管理员）进入同一场比赛详情
4. 用户 B 点击用户 A 的 **"查看截图"**
5. 验证：应能正常显示用户 A 的截图，不再是空白

**场景：管理员查看所有截图**

1. 管理员进入比赛详情
2. 点击任意用户的 **"查看截图"**
3. 验证：所有截图都能正常显示

**预期问题排查**：
- 如果仍然空白：截图可能是网络图片（http URL），检查控制台是否有跨域错误
- 如果显示模糊：检查 `getTempFileURL` 是否成功返回了临时 URL

---

### 测试 3：编辑模式字段完整性（create.vue）

1. 已注册用户进入 **"我的" → "注册成为球员"**
2. 检查以下字段是否都能显示和修改：
   - ✅ 头像（可点击修改）
   - ✅ 姓名（文本输入）
   - ✅ 昵称（文本输入，支持微信昵称获取）
   - ✅ 康体通后四位（数字输入）
   - ✅ 出生年月（日期选择器）
   - ✅ 评分权限（开关选择：允许/不接受）
   - ✅ 擅长位置（多选网格）
   - ✅ 初始评分（滑动条）
3. 修改任意字段后保存
4. 验证：重新进入页面，修改应已保留

---

## 三、修改内容汇总

### `pages/players/create.vue`

| 修改项 | 说明 |
|--------|------|
| 昵称输入框 | 添加 `type="nickname"`，支持微信昵称一键获取 |
| 头像选择 | 替换 `wx.getUserProfile`（已废弃）为 `wx.chooseAvatar` |
| `checkUser` | 加载时添加 `avatar` 字段 |
| `updateMyPlayer` | 保存时添加 `avatar` 字段 |
| 编辑模式 | 已添加头像、出生年月、评分权限显示 |

### `pages/match/detail.vue`（已在前次部署）

| 修改项 | 说明 |
|--------|------|
| `viewScreenshot` | 使用 `getTempFileURL` 将 `cloud://` fileID 转换为临时 URL |

---

## 四、常见问题 FAQ

**Q1：为什么 `wx.getUserProfile` 不能用了？**
> 微信在 2022 年 10 月后回收了 `getUserProfile` 接口。必须使用 `wx.chooseAvatar` 获取头像、`type="nickname"` 输入框获取昵称。

**Q2：截图显示空白是什么原因？**
> 云存储的 `cloud://` 文件路径不能直接用于 `<image>` 标签显示。必须先通过 `getTempFileURL` 转换为临时 `https://` URL。已在前次部署中修复。

**Q3：编辑模式为什么不显示头像？**
> 之前的 `checkUser` 方法没有加载 `avatar` 字段，本次已修复。现在编辑模式会正确加载并显示用户已设置的头像。

---

## 五、回滚方案

如果部署后出现问题，可以：

1. 在 HBuilderX 中右键 `create.vue` → **本地历史** → 回滚到修改前的版本
2. 重新编译上传
3. 或直接在微信开发者工具中上传之前的体验版代码

---

*生成时间：2026-07-02*
