# 部署与测试指南：头像/消息推送/24小时退出修复

## 本次修复的五个问题

1. **用户注册时选择微信头像不能成功填入** — 使用 `button open-type="chooseAvatar"` 组件替代 `wx.chooseAvatar` JS API
2. **6种消息推送场景配置** — 完善所有业务触点的消息通知
3. **24小时内退出警告和名单显示** — 弹窗警告 + 管理员可见的迟到退出名单
4. **创建/修改比赛后通知所有用户** — 已在新比赛创建时实现，本次补充编辑模式
5. **赛前24h/3h提醒** — 场主和护法接收提醒

---

## 一、部署步骤

### 步骤 1：重新编译上传客户端代码

在 HBuilderX 中：
1. 找到以下文件，确保已保存：
   - `pages/players/create.vue`（头像选择修复）
   - `pages/match/detail.vue`（24小时退出 + 评分通知）
   - `pages/match/create.vue`（编辑比赛通知）
2. 上传流程：
   - 工具栏点击 **运行 → 运行到小程序模拟器 → 微信开发者工具**
   - 在微信开发者工具中：点击 **上传** → 填写版本号（如 `1.2.4`）→ 确定

### 步骤 2：部署云函数（按顺序）

在微信开发者工具中，对每个云函数右键 → **创建并部署：云端安装依赖**：

1. `sendNotification` — 消息通知核心云函数（已更新）
2. `cancelRegistration` — 取消报名 + 24小时退出记录（已更新）
3. `sendReminders` — 赛前定时提醒（已更新，含定时触发器）
4. `registerMatch` — 报名处理（如上次未部署）

> `sendReminders` 的 `config.json` 已配置每小时触发（`0 0 * * * * *`），部署后自动生效。

### 步骤 3：提交审核（正式版）

1. 登录[微信公众平台](https://mp.weixin.qq.com)
2. 进入 **版本管理**
3. 找到上传的版本 → **提交审核**
4. 审核通过后 → **发布**

---

## 二、消息推送场景汇总

| 编号 | 激活事项 | 接收人 | 内容 | 触发点 | 类型 |
|------|---------|--------|------|--------|------|
| 1 | 创建/修改比赛 | 所有用户 | "新场次已创建，快来报名吧" / "场次信息已更新" | `create.vue` 提交后 | `new_match` |
| 2 | 确认名单 | 所有已确认队员 | "您已成功进入比赛名单" | `detail.vue` 确认名单后弹窗发送 | `confirmed` |
| 3 | 候补晋升 | 候补成功队员 | "您已成功候补进入比赛名单" | `cancelRegistration` 云函数自动触发 | `promoted` |
| 4 | 分队完成 | 所有已确认队员 | "分队已完成，您被分入X队，队长是XX，请进入场次页面查看" | `teamSplit.vue` 保存后弹窗发送 | `team_split` |
| 5 | 开启评分 | 所有已确认队员 | "XX场次评分已开放，请进入场次页面进行队友互评" | `detail.vue` 开启评分后自动发送 | `rating_open` |
| 6 | 赛前24h/3h | 场主及护法 | "XX场次即将于XX时间后开始，您是本场场主/护法，请携带身份证到场" | `sendReminders` 定时云函数每小时自动检查 | `reminder` |

---

## 三、测试验证

### 测试 1：微信头像选择（create.vue）

**场景：新用户注册**

1. 使用未注册账号进入小程序
2. 进入 **"我的" → "注册成为球员"**
3. 检查头像区域：
   - 左侧：点击"从相册选择" → 应能正常选择图片并上传
   - 右侧："使用微信头像"按钮 → 点击后应弹出微信头像选择界面
4. 选择微信头像后，头像应显示在表单中
5. 填写昵称（点击输入框，看是否有"使用微信昵称"提示）
6. 提交注册
7. 验证：球员名单中能看到新用户的头像

**场景：已有用户修改头像**

1. 已注册用户进入 **"我的" → "注册成为球员"**
2. 点击"使用微信头像" → 选择新头像
3. 保存修改
4. 验证：重新进入页面，头像已更新

**预期问题排查**：
- 如果"使用微信头像"无反应：检查微信基础库版本 ≥ 2.21.2
- 如果上传失败：检查云存储权限和 `uploadFile` 配额

---

### 测试 2：消息推送

**场景 1：创建比赛通知**

1. 管理员创建一个新比赛
2. 检查所有已注册用户是否收到订阅消息通知
3. 如果用户未授权订阅消息，通知会失败（这是正常的）

**场景 2：确认名单通知**

1. 管理员进入比赛详情 → 点击"确认名单"
2. 确认后弹窗询问是否发送通知
3. 点击"发送" → 自定义消息内容（留空则使用默认"您已成功进入比赛名单"）
4. 验证：已确认队员收到通知

**场景 3：候补晋升通知**

1. 让已确认队员退出比赛（制造空位）
2. 候补队员应自动晋升
3. 验证：晋升的候补队员收到"您已成功候补进入比赛名单"通知

**场景 4：分队通知**

1. 管理员进入分队页面 → 调整分队 → 保存
2. 弹窗询问是否发送分队通知
3. 点击"发送" → 自定义消息内容（留空则使用默认分队信息）
4. 验证：所有已确认队员收到分队通知，包含队名和队长信息

**场景 5：开启评分通知**

1. 管理员完成比赛记录
2. 点击"开启评分"
3. 验证：所有已确认队员收到"评分已开放，请进入场次页面进行队友互评"通知

**场景 6：赛前提醒**

1. 创建一个即将在2小时后开始的比赛
2. 设置场主和护法
3. 确认名单
4. 等待 `sendReminders` 定时触发（或手动调用云函数测试）
5. 验证：场主和护法收到赛前提醒通知

> 手动测试 `sendReminders`：在微信开发者工具 → 云开发 → 云函数 → `sendReminders` → 测试 → 使用空参数 `{}` 调用

---

### 测试 3：24小时内退出

**场景：正常退出（24小时外）**

1. 创建一个明天晚上的比赛
2. 用户报名并确认
3. 用户点击"退出"
4. 验证：弹窗显示"确定退出这场报名？"（无警告）
5. 确认后正常退出

**场景：24小时内退出**

1. 创建一个2小时后开始的比赛
2. 用户报名并确认
3. 用户点击"退出"
4. 验证：弹窗显示 ⚠️ "距离比赛开始已不足24小时，退出可能会被罚款，是否确认？"
5. 点击"取消" → 不退出
6. 再次点击"退出" → 点击"确认退出"
7. 验证：用户成功退出，但管理员在比赛详情页能看到"⚠️ 开始前24小时内退出名单"
8. 名单中显示该用户的昵称和退出时间

**场景：管理员查看迟到退出名单**

1. 管理员进入比赛详情
2. 在"参赛名单"下方，应能看到"⚠️ 开始前24小时内退出名单"区域
3. 验证：名单中显示所有24小时内退出的队员昵称和退出时间

---

## 四、修改内容汇总

### `pages/players/create.vue`

| 修改项 | 说明 |
|--------|------|
| 头像选择区域 | 拆分为"从相册选择" + "使用微信头像"按钮（`button open-type="chooseAvatar"`） |
| `chooseAvatarFromAlbum` | 从相册选择的独立方法 |
| `onChooseAvatar` | 处理微信头像选择事件，上传至云存储 |
| 样式 | 新增 `avatar-row`、`.avatar-wechat-btn` 样式 |

### `pages/match/detail.vue`

| 修改项 | 说明 |
|--------|------|
| `toggleRating` | 开启评分后自动发送 `rating_open` 通知 |
| `unregister` | 添加24小时检查逻辑，24小时内退出显示警告弹窗 |
| `doUnregister` | 退出逻辑提取，支持 `isLateQuit` 参数 |
| `lateQuitters` 显示 | 管理员可见的24小时内退出名单区域 |
| 样式 | 新增 `.late-quitter-item`、`.late-quitter-name`、`.late-quitter-time` |

### `pages/match/create.vue`

| 修改项 | 说明 |
|--------|------|
| 编辑模式 | 保存修改后发送 `new_match` 通知（自定义消息："场次信息已更新"） |

### `cloudfunctions/sendNotification/index.js`

| 修改项 | 说明 |
|--------|------|
| `rating_open` | 新增类型：评分开放通知 |
| `team_split` | 改进：自动包含分队信息和队长信息 |
| `reminder` | 改进：自动构建包含比赛名称、时间、角色的提醒消息 |
| `confirmed` | 默认消息改为"您已成功进入比赛名单" |
| `promoted` | 默认消息改为"您已成功候补进入比赛名单" |

### `cloudfunctions/cancelRegistration/index.js`

| 修改项 | 说明 |
|--------|------|
| 24小时限制 | 取消硬性阻止，改为允许但记录 |
| `lateQuitters` | 新增字段：记录24小时内退出的队员 |
| `isLateQuit` | 新增参数：标记是否为24小时内退出 |

### `cloudfunctions/sendReminders/index.js`

| 修改项 | 说明 |
|--------|------|
| 消息内容 | 使用 `customMessage` 参数，内容："XX场次即将于XX时间后开始，您是本场场主/护法，请携带身份证到场" |

---

## 五、常见问题 FAQ

**Q1：为什么用户收不到消息通知？**
> 微信小程序的订阅消息需要用户**主动授权**。用户首次收到通知时会弹窗询问是否允许，如果拒绝则后续无法收到。建议在比赛页面添加提示，引导用户点击授权。

**Q2：赛前提醒为什么不发送？**
> `sendReminders` 是**定时云函数**，每小时执行一次。检查：
> 1. 云函数是否已部署（含 `config.json` 定时触发器）
> 2. 比赛是否已确认名单（`teamConfirmed: true`）
> 3. 比赛状态是否为 `upcoming` 或 `ongoing`
> 4. 是否已发送过（检查 `reminder24hSent` / `reminder3hSent` 字段）

**Q3：24小时内退出名单为什么不显示？**
> 检查：
> 1. 用户退出时是否距离比赛开始不足24小时
> 2. 用户是否点击了"确认退出"（不是取消）
> 3. 管理员是否已刷新页面（`lateQuitters` 是实时数据）

**Q4：头像上传失败怎么办？**
> 1. 检查云存储配额是否已满
> 2. 检查 `wx.chooseAvatar` 基础库版本要求（≥ 2.21.2）
> 3. 从相册选择作为备选方案

---

## 六、回滚方案

如果部署后出现问题，可以：

1. 在 HBuilderX 中右键修改的文件 → **本地历史** → 回滚到修改前的版本
2. 重新编译上传
3. 云函数回滚：在微信开发者工具中删除当前版本，重新上传旧版本代码

---

*生成时间：2026-07-02*
