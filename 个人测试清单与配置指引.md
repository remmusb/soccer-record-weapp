# 野球记录小程序 - 个人测试清单 & 配置指引

> 在你拉上测试团队之前，先一个人把所有功能跑通一遍。下面是单人可完成的完整测试清单。

---

## 一、个人测试前必须完成的配置

### 1.1 部署全部云函数（微信开发者工具）

对每个云函数目录，右键 → **创建并部署：云端安装依赖**

| 云函数 | 说明 | 是否必须 |
|--------|------|----------|
| `addAdmin` | 添加管理员 | ✅ |
| `cancelRegistration` | 取消报名 | ✅ |
| `createPlayer` | 创建球员（管理员） | ✅ |
| `deletePlayer` | 删除球员 | ✅ |
| `exportData` | 导出数据 | 可选 |
| `getAdmins` | 获取管理员 | ✅ |
| `getPlayerMatches` | 获取球员场主/护法比赛 | ✅ |
| `getPlayers` | 获取所有球员 | ✅ |
| `login` | 登录获取身份 | ✅ |
| `processRegistration` | 确认名单排序 | ✅ |
| `recalculateStats` | 重新计算统计 | ✅ |
| `removeAdmin` | 移除管理员 | ✅ |
| `sendNotification` | 发送通知 | 可选（需模板ID） |
| `sendReminders` | 定时赛前提醒 | 可选（需模板ID+定时器） |

### 1.2 创建数据库集合

登录 [微信云开发控制台](https://console.cloud.tencent.com/tcb) → 数据库 → 创建集合：

| 集合名 | 用途 |
|--------|------|
| `players` | 球员资料 |
| `matches` | 比赛场次 |
| `admins` | 管理员列表 |
| `settings` | 系统设置 |

### 1.3 初始化管理员

在数据库 `admins` 集合中插入一条记录：

```json
{
  "playerId": "你的playerId",
  "createdAt": {
    "$date": "2025-06-17T00:00:00.000Z"
  }
}
```

> 你的 `playerId` 需要先创建自己的球员资料后，在 `players` 集合中找到 `_id` 字段。

### 1.4 数据库权限设置

对每个集合，点击 **权限设置** → 选择 **"所有用户可读，仅创建者可写"**

> 如果不改，客户端可能查不到数据。

---

## 二、个人测试Checklist（一个人就能完成）

### 阶段1：创建球员资料

- [ ] **1.1 为自己创建球员**
  - 进入"球员"页 → 填写姓名/昵称/位置/初始评分
  - 测试头像：从相册上传一张 + 测试微信头像获取
  - 验证：列表页显示头像、昵称、综合评分（初始分7应显示6.6）

- [ ] **1.2 创建7-8个测试球员**
  - 用管理员身份为他人创建（填写不同位置：GK、CB、CM、ST等）
  - 验证：每个球员在列表中正常显示

- [ ] **1.3 验证球员详情**
  - 点击任意球员 → 检查：头像、生涯数据（出场0/进球0/助攻0/场主0/护法0）
  - 切换全部/本年度/本月 → 数据正确变化

- [ ] **1.4 验证删除球员**
  - 管理员删除一个球员 → 验证从列表消失
  - 删除后该球员已参加的比赛记录仍保留（不能回滚）

### 阶段2：创建比赛

- [ ] **2.1 创建需抽场的比赛**
  - 选择日期 → 验证结束时间自动 = 开始时间+1.5小时
  - 输入地点 → 测试地图选点（如果失败则手动输入）
  - 验证截图截止时间默认 = 比赛日前13天24点
  - 手动修改截图截止时间 → 验证保存

- [ ] **2.2 创建无需抽场的比赛**
  - 切换"是否需要抽场"为关闭
  - 验证创建后没有截图截止时间显示

- [ ] **2.3 验证首页显示**
  - 两个比赛都在首页显示
  - 验证：场主信息（如果有）、报名人数、状态标签

### 阶段3：报名流程

- [ ] **3.1 普通球员报名（自己）**
  - 进入需抽场比赛 → 点击"+报名" → 选择自己
  - 如果截图截止时间未到 → 状态应为"pending_screenshot"，提示"请尽快上传截图"
  - 如果截图截止时间已过 → 状态应为"confirmed"，直接确认

- [ ] **3.2 管理员为他人报名**
  - 选择其他球员 → 验证状态正确
  - 验证：报名人数实时更新

- [ ] **3.3 超过人数上限测试**
  - 继续报名直到超过 `maxPlayers`（默认14）
  - 第15人报名 → 状态应为"WL"（候补）

- [ ] **3.4 上传截图**
  - 点击"上传截图" → 选择图片 → 上传成功
  - 验证状态从"pending_screenshot"变为"confirmed"
  - 点击"查看截图" → 正常显示

- [ ] **3.5 取消报名**
  - 比赛前24小时以上取消 → 成功，人数减少
  - 比赛前24小时内取消 → 提示不可取消

### 阶段4：管理员操作

- [ ] **4.1 设定场主**
  - 点击场主行"设定" → 选择球员 → 验证👑标签出现
  - 点击"更换" → 选择新球员 → 验证更换成功

- [ ] **4.2 添加护法**
  - 点击护法行"添加" → 选择球员 → 验证🛡️标签出现
  - 添加第2、3、4个护法 → 都成功
  - 添加第5个护法 → 提示"护法最多4人"

- [ ] **4.3 移除护法**
  - 点击某个护法"移除" → 确认 → 验证从列表消失

- [ ] **4.4 关闭/开启报名**
  - 点击"关闭报名" → 普通用户看不到"+报名"按钮
  - 点击"开启报名" → 按钮恢复
  - 关闭状态下管理员仍可见"+添加球员"按钮

- [ ] **4.5 确认名单**
  - 点击"确认名单" → 验证：场主/护法优先级最高，有截图优先，按报名时间
  - 超出maxPlayers的变为"WL"候补
  - 点击"重新确认名单" → 验证可以重新排序

- [ ] **4.6 编辑比赛**
  - 点击"编辑信息" → 修改标题/地点 → 保存 → 验证更新

- [ ] **4.7 删除比赛**
  - 创建一场测试比赛 → 删除 → 验证从首页消失

### 阶段5：分队功能

- [ ] **5.1 自动分队**
  - 确认名单后 → 点击"自动分队"
  - 验证：两队人数均衡（差不超过1），平均分低的队多一人
  - 验证：有门将时各队1个，后场:中场:前锋 ≈ 2:2:1

- [ ] **5.2 阵型图**
  - 验证阵型图显示：绿色足球场背景、白色中线、门将→后场→中场→前锋
  - 每个位置显示球员头像+昵称

- [ ] **5.3 设定队服颜色**
  - A队输入"红色" → B队输入"蓝色" → 保存
  - 验证对阵信息中显示颜色标签

- [ ] **5.4 手动调整分队**
  - 把某个球员从A移到B → 验证阵型图实时更新

- [ ] **5.5 保存分队**
  - 点击保存 → 返回比赛详情 → 验证对阵区域显示分队结果

### 阶段6：比赛流程

- [ ] **6.1 开始比赛**
  - 点击"开始比赛" → 状态变为"ongoing"
  - 验证"记录赛况"按钮出现

- [ ] **6.2 记录赛况**
  - 进入记录赛况 → 修改比分（A:2 B:1）
  - 添加事件：球员A进球、球员B黄牌
  - 保存 → 验证成功

- [ ] **6.3 结束比赛**
  - 点击"结束比赛" → 状态变为"completed"
  - 验证统计自动更新（球员出场+1、进球+1等）

- [ ] **6.4 重新开启**
  - 已结束比赛 → 点击"重新开启" → 状态变回"upcoming"

### 阶段7：评分功能

- [ ] **7.1 评分窗口开放**
  - 结束时间后 → 验证"赛后评分"按钮出现
  - 24小时后 → 验证按钮消失

- [ ] **7.2 队友互评**
  - 进入评分页 → 验证名单中所有球员（不限于同队）都可评
  - 给多个球员打分 → 提交

- [ ] **7.3 管理员评分**
  - 管理员进入评分页 → 验证可以给所有球员评分
  - 提交 → 验证综合评分更新

- [ ] **7.4 验证评分结果**
  - 进入球员详情 → 评分历史显示最新评分
  - 综合评分 = 队友×50% + 系统×30% + 表现×20%

### 阶段8：统计功能

- [ ] **8.1 重新计算统计**
  - 进入设置页 → 点击"重新计算统计"
  - 验证所有球员评分更新正确

- [ ] **8.2 验证场主/护法统计**
  - 场主过几次 → 球员详情场主次数正确
  - 护法过几次 → 球员详情护法次数正确

- [ ] **8.3 首页显示验证**
  - 每个场次显示场主（👑）和护法（🛡️）
  - 多个护法用顿号分隔

---

## 三、所有需要发送消息的功能列表

### 3.1 通知类型总览

| 通知类型 | 触发时机 | 接收人 | 内容 | 是否需要模板ID |
|----------|----------|--------|------|----------------|
| **名单确认** | 管理员确认名单后 | 所有confirmed球员 | "您已成功报名，请准时参加" | 是 |
| **候补晋升** | 候补球员因他人退出进入confirmed | 晋升的球员 | "恭喜从候补进入正式名单" | 是 |
| **场主/护法设定** | 设定场主或护法时 | 被设定的人 | "您已被设定为场主/护法" | 是 |
| **分队结果** | 保存分队后 | 所有分队球员 | "您分在A队（红色），队长：张三" | 是 |
| **赛前24h提醒** | 比赛前24小时（自动） | 场主+所有护法 | "比赛24小时后开始，请签场" | 是 |
| **赛前3h提醒** | 比赛前3小时（自动） | 场主+所有护法 | "比赛3小时后开始，请签场" | 是 |

### 3.2 模板ID配置（必须完成）

#### 步骤1：申请模板
1. 登录 [微信公众平台](https://mp.weixin.qq.com/)
2. 功能 → 订阅消息 → 我的模板 → 添加模板
3. 搜索关键词，选择包含以下字段的模板：
   - **模板A（比赛通知）**：`thing1`（事物名称）、`time2`（时间）、`thing3`（事物内容）
   - **模板C（分队通知）**：`thing1`（事物名称）、`thing2`（事物内容）、`thing3`（事物内容）

4. 记录模板ID（形如 `xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx`）

#### 步骤2：替换代码中的占位符

打开以下文件，替换 `YOUR_TEMPLATE_ID_A` 和 `YOUR_TEMPLATE_ID_C`：

| 文件 | 替换位置 |
|------|----------|
| `cloudfunctions/sendNotification/index.js` 第9行 | `YOUR_TEMPLATE_ID_A` → 模板A的ID |
| `cloudfunctions/sendNotification/index.js` 第10行 | `YOUR_TEMPLATE_ID_C` → 模板C的ID |

替换后重新部署 `sendNotification` 云函数。

#### 步骤3：用户首次授权

用户首次触发通知时（如报名、被设定为场主），小程序会弹出订阅授权弹窗。用户点击"允许"后，后续才能收到通知。

> ⚠️ 如果用户拒绝授权，通知无法发送。需要引导用户在设置中重新开启。

### 3.3 定时提醒配置（可选）

`sendReminders` 云函数每小时自动运行，检查即将开始的比赛并发送提醒。

#### 配置步骤：

1. 部署 `sendReminders` 云函数
2. 登录 [腾讯云云函数控制台](https://console.cloud.tencent.com/scf)
3. 找到 `sendReminders` 函数 → 触发管理 → 新建触发器
4. 触发方式：定时触发
5. 触发周期：`0 */1 * * * *`（每小时执行一次）
6. 保存

> 如果不配置，定时提醒不会自动发送，但手动触发可以测试。

---

## 四、开通地图接口指引

### 4.1 为什么需要开通

`wx.chooseLocation` 接口需要微信官方审核通过才能使用。未开通时调用会报错。

### 4.2 开通步骤

1. 登录 [微信公众平台](https://mp.weixin.qq.com/)
2. 开发 → 开发管理 → 接口设置
3. 找到 **"地理位置"** 相关接口（如 `wx.chooseLocation`）
4. 点击 **"申请开通"**
5. 填写使用场景：
   - 接口用途：获取球场位置信息，方便球员导航
   - 使用页面：创建比赛页面
   - 使用频次：用户主动点击时触发
6. 提交审核，通常1-2个工作日通过

### 4.3 临时替代方案

在审核通过前，地点可以手动输入。地图选点只是辅助功能，不影响创建比赛。

---

## 五、已知待办事项（上线前必须完成）

| 优先级 | 事项 | 操作步骤 | 影响 |
|--------|------|----------|------|
| 🔴 **高** | 模板ID替换 | 申请模板 → 替换代码 → 重新部署 `sendNotification` | 不完成则所有通知无法发送 |
| 🔴 **高** | 数据库权限 | 每个集合设为"所有用户可读，仅创建者可写" | 不完成则数据查不到 |
| 🟡 **中** | 地图接口开通 | 微信公众平台申请地理位置接口 | 不完成则地图选点失败，但可手动输入 |
| 🟡 **中** | 定时触发器 | 腾讯云控制台配置 `sendReminders` 每小时触发 | 不完成则赛前自动提醒不发送 |
| 🟢 **低** | 小程序改名 | 从"欢足报名统计"改为"欢期"，需微信审核 | 当前名称可用，不影响功能 |
| 🟢 **低** | 添加体验成员 | 微信公众平台添加测试人员微信号 | 不添加则无法扫码体验 |

---

## 六、问题排查速查表

| 现象 | 可能原因 | 解决方法 |
|------|----------|----------|
| 点击报名没反应 | `allPlayers`为空或报错被吞 | 检查`getPlayers`云函数是否部署，检查数据库权限 |
| 页面白屏 | `endTime`格式问题 | 检查数据库中`endTime`是`HH:mm`还是完整日期时间 |
| 云函数报错"Cannot find module" | 缺少`package.json`或未安装依赖 | 右键云函数 → 创建并部署：云端安装依赖 |
| 头像不显示 | 云存储fileID错误或avatar字段未保存 | 检查创建球员时avatar是否正确写入 |
| 评分不更新 | `recalculateStats`崩溃或`?.`语法错误 | 查看云函数日志，检查Node.js版本 |
| 看不到管理员按钮 | 未在`admins`集合添加记录 | 数据库手动添加管理员记录 |
| 通知发送失败 | 模板ID未替换或用户未授权 | 替换模板ID，引导用户授权订阅消息 |
| 地图选点失败 | 接口未开通或权限未授权 | 申请地理位置接口，或引导用户开启位置权限 |

---

**完成以上所有测试后，记录发现的问题，把截图和描述发给我，我逐个修复。**
