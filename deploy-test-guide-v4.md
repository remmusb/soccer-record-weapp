# 部署与测试指南：自动消息 + 队名修复

## 本次修复的三个问题

1. **所有消息自动发送**：取消手动弹窗确认，6种消息按固定模板自动生成并发送
2. **消息模板内容**：使用【场次信息】等变量自动生成内容
3. **分队队名显示**：使用创建比赛时设定的队名（如 🔴 红队 / 🔵 蓝队），不显示硬编码的 "A队"/"B队"

---

## 一、部署步骤

### 步骤 1：部署云函数（按顺序）

在微信开发者工具中，对每个云函数右键 → **创建并部署：云端安装依赖**：

1. **`sendNotification`** — 消息通知核心云函数（已更新消息模板）
2. **`sendReminders`** — 赛前定时提醒（已更新时间标签参数）
3. **`updateMatch`** — 通用更新比赛信息（新创建）
4. **`cancelRegistration`** — 取消报名 + 24小时退出记录（如有未部署）
5. **`registerMatch`** — 报名处理（如有未部署）

> `sendReminders` 的 `config.json` 已配置每小时触发（`0 0 * * * * *`），部署后自动生效。

### 步骤 2：重新编译上传客户端代码

在 HBuilderX 中：
1. 找到以下文件，确保已保存：
   - `pages/match/detail.vue`（自动消息 + 队名显示）
   - `pages/match/create.vue`（默认队名改为 🔴 红队 / 🔵 蓝队）
   - `pages/match/teamSplit.vue`（分队保存 + 队名显示）
   - `pages/match/record.vue`（队名显示）
   - `pages/players/create.vue`（头像修复）
2. 上传流程：
   - 工具栏点击 **运行 → 运行到小程序模拟器 → 微信开发者工具**
   - 在微信开发者工具中：点击 **上传** → 填写版本号（如 `1.2.6`）→ 确定

### 步骤 3：提交审核（正式版）

1. 登录[微信公众平台](https://mp.weixin.qq.com)
2. 进入 **版本管理**
3. 找到上传的版本 → **提交审核**
4. 审核通过后 → **发布**

---

## 二、消息推送场景汇总（全部自动发送）

| 编号 | 激活事项 | 接收人 | 内容模板 | 触发点 |
|------|---------|--------|----------|--------|
| 1 | 创建/修改比赛 | 所有用户 | "新场次已创建，【场次信息】，请进入小程序报名" | 创建/编辑比赛后自动发送 |
| 2 | 确认名单 | 已确认队员 | "您已成功进入【场次信息】比赛名单" | 管理员确认名单后自动发送 |
| 3 | 候补晋升 | 候补成功队员 | "您已成功候补进入【场次信息】比赛名单" | 有人退出后自动晋升时自动发送 |
| 4 | 分队完成 | 已确认队员 | "分队已完成，您被分入【队名】队，队长是【队长名】，请进入场次页面查看" | 保存分队后自动发送 |
| 5 | 开启评分 | 已确认队员 | "【场次信息】评分已开放，请进入场次页面进行队友互评" | 管理员开启评分后自动发送 |
| 6 | 赛前24h/3h | 场主及护法 | "【场次信息】即将于【24小时/3小时】后开始，您是本场场主/护法，请携带身份证到场" | 定时触发器自动发送 |

> 注：所有消息均为自动发送，管理员无需手动确认。订阅消息需要用户提前授权，未授权用户不会收到。

---

## 三、测试验证

### 测试 1：消息自动发送

**场景：创建比赛**

1. 管理员创建一个新比赛
2. 检查所有已注册用户是否收到订阅消息通知
3. 消息内容应包含："新场次已创建，【场次信息】，请进入小程序报名"

**场景：确认名单**

1. 管理员进入比赛详情 → 点击"确认名单"
2. 确认后，已确认队员应自动收到通知
3. 消息内容："您已成功进入【场次信息】比赛名单"

**场景：分队通知**

1. 管理员进入分队页面 → 调整分队 → 保存
2. 保存后，所有已确认队员应自动收到分队通知
3. 消息内容："分队已完成，您被分入【队名】队，队长是【队长名】..."

**场景：开启评分**

1. 管理员完成比赛记录 → 点击"开启评分"
2. 所有已确认队员自动收到评分开放通知

**场景：赛前提醒**

1. 创建一个即将在2小时后开始的比赛
2. 设置场主和护法，确认名单
3. 等待 `sendReminders` 定时触发（或手动调用测试）
4. 场主和护法收到提醒："【场次信息】即将于3小时后开始..."

> 手动测试 `sendReminders`：在微信开发者工具 → 云开发 → 云函数 → `sendReminders` → 测试 → 使用空参数 `{}` 调用

---

### 测试 2：队名显示

**场景：分队页面队名**

1. 管理员创建比赛，设定队名（如 🔴 红队 / 🔵 蓝队）
2. 进入分队页面
3. 验证：页面顶部显示设定的队名，不显示 "A队"/"B队"

**场景：比赛详情分队显示**

1. 进入已分队的比赛详情
2. 验证：分队区域显示设定的队名（如 🔴 红队）

**场景：报名列表队名标签**

1. 进入比赛详情 → 查看报名接龙
2. 已分队的队员旁边显示队名标签（如 🔴 红队）

**场景：赛况记录队名**

1. 管理员进入"记录赛况"页面
2. 验证：比分区域显示设定的队名，不显示 "A队"/"B队"

---

## 四、修改内容汇总

### `cloudfunctions/sendNotification/index.js`

| 修改项 | 说明 |
|--------|------|
| `new_match` | 默认消息："新场次已创建，【场次信息】，请进入小程序报名" |
| `confirmed` | 默认消息："您已成功进入【场次信息】比赛名单" |
| `promoted` | 默认消息："您已成功候补进入【场次信息】比赛名单" |
| `team_split` | 使用 `teamSplit` 模板，thing2: 队名，thing3: 队长名 |
| `rating_open` | 默认消息："【场次信息】评分已开放，请进入场次页面进行队友互评" |
| `reminder` | 使用外部传入的时间标签（24小时/3小时） |

### `cloudfunctions/sendReminders/index.js`

| 修改项 | 说明 |
|--------|------|
| `sendReminder` | 传入 `customMessage` 作为时间标签（"24小时"或"3小时"） |

### `pages/match/detail.vue`

| 修改项 | 说明 |
|--------|------|
| `confirmTeam` | 确认名单后**自动发送**通知，不再弹窗确认 |
| 分队标签 | 报名列表中的队名标签使用 `match.teamA.name` / `match.teamB.name` |
| 分队显示 | 分队区域使用设定的队名 |

### `pages/match/teamSplit.vue`

| 修改项 | 说明 |
|--------|------|
| `save` | 保存分队后**自动发送**通知，不再弹窗确认；使用 `updateMatch` 云函数 |
| `saveTeams` | 使用 `updateMatch` 云函数替代客户端直接写入 |
| 队名标题 | 分队标题使用 `match.teamA.name` / `match.teamB.name` |
| 阵型标题 | 阵型标题使用设定的队名 |

### `pages/match/create.vue`

| 修改项 | 说明 |
|--------|------|
| 默认队名 | `teamAName` 改为 `'🔴 红队'`，`teamBName` 改为 `'🔵 蓝队'` |
| 编辑通知 | 修改比赛后自动发送通知 |

### `pages/match/record.vue`

| 修改项 | 说明 |
|--------|------|
| 比分标签 | 去掉硬编码 "A队"/"B队"，只显示设定的队名 |

### 新创建：`cloudfunctions/updateMatch/index.js`

通用更新比赛信息云函数，接收 `matchId` 和 `updateData`，使用管理员权限更新数据库。

---

## 五、常见问题 FAQ

**Q1：为什么用户收不到消息通知？**
> 微信小程序的订阅消息需要用户**主动授权**。用户首次收到通知时会弹窗询问是否允许，如果拒绝则后续无法收到。建议在比赛页面添加提示，引导用户点击授权。

**Q2：分队通知为什么不发送？**
> 检查：
> 1. `updateMatch` 云函数是否已部署
> 2. 分队时是否有已确认队员
> 3. 订阅消息模板 ID 是否正确配置

**Q3：队名为什么不显示？**
> 检查：
> 1. 创建比赛时是否设定了队名（默认 🔴 红队 / 🔵 蓝队）
> 2. 旧数据可能没有 `teamA.name`/`teamB.name` 字段，会回退到 "A队"/"B队"

---

## 六、回滚方案

如果部署后出现问题：

1. 在 HBuilderX 中右键修改的文件 → **本地历史** → 回滚到修改前的版本
2. 重新编译上传
3. 云函数回滚：在微信开发者工具中删除当前版本，重新上传旧版本代码

---

*生成时间：2026-07-02*
