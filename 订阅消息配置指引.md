# 野球记录小程序 - 订阅消息配置指引

## 一、概述

本小程序使用微信**订阅消息**（一次性订阅）向用户推送通知。用户每次需主动授权，授权一次可发送一条消息。

---

## 二、需要发送的消息场景

| 场景 | 触发时机 | 接收人 | 内容概要 |
|------|----------|--------|----------|
| **名单确认通知** | 管理员点击"确认比赛名单"后 | 所有进入confirmed名单的队员 | 恭喜成功报名，比赛时间地点 |
| **候补晋升通知** | 有队员退出，WL队员自动晋升 | 晋升的候补队员 | 恭喜从候补进入正式名单 |
| **场主/护法设定通知** | 管理员设定场主/护法后 | 被设定的场主/护法 | 您已被设定为场主/护法，请签场 |
| **赛前24小时提醒** | 比赛前24小时自动触发 | 场主、护法 | 比赛24小时后开始，请签场 |
| **赛前3小时提醒** | 比赛前3小时自动触发 | 场主、护法 | 比赛3小时后开始，请签场 |
| **分队结果通知** | 分队完成后（手动/自动） | 所有参赛队员 | 分队结果、队服颜色、队长 |
| **评分窗口开启通知** | 比赛结束时间后 | 参赛队员 | 赛后评分已开启，7天内有效 |

---

## 三、后台配置步骤

### 1. 登录微信公众平台

地址：https://mp.weixin.qq.com

用小程序管理员微信扫码登录。

### 2. 开通订阅消息

左侧菜单 → **功能** → **订阅消息** → 点击"**开通**"

### 3. 添加模板

点击"**公共模板库**"，搜索以下关键词（不同类目可用模板不同，选最接近的）：

#### 模板A：比赛通知（用于名单确认、候补晋升、场主设定）

**搜索关键词**：`活动提醒`、`报名成功`、`任务通知`

**推荐模板字段**：
| 字段 | 类型 | 示例值 | 用途 |
|------|------|--------|------|
| thing1 | 事物 | 周中野球局 | 比赛名称 |
| time2 | 时间 | 2025-06-28 20:00 | 比赛时间 |
| thing3 | 事物 | 恭喜成功报名！ | 通知内容 |

**模板ID示例**：`xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx`（复制后台实际ID）

#### 模板B：赛前提醒（用于24h/3h提醒）

**搜索关键词**：`预约提醒`、`待办提醒`、`签到提醒`

**推荐模板字段**：
| 字段 | 类型 | 示例值 | 用途 |
|------|------|--------|------|
| thing1 | 事物 | 周中野球局 | 比赛名称 |
| time2 | 时间 | 2025-06-28 20:00 | 比赛时间 |
| thing3 | 事物 | 请提前签场 | 提醒内容 |

#### 模板C：分队通知（用于分队结果）

**搜索关键词**：`活动通知`、`结果通知`

**推荐模板字段**：
| 字段 | 类型 | 示例值 | 用途 |
|------|------|--------|------|
| thing1 | 事物 | 周中野球局 | 比赛名称 |
| thing2 | 事物 | A队（红色） | 分队信息 |
| thing3 | 事物 | 队长：张三 | 队长信息 |

---

## 四、代码配置

### 1. 填写模板ID

打开以下文件，将 `templateId` 替换为你在后台复制的真实模板ID。

#### `cloudfunctions/sendNotification/index.js`

```javascript
const cloud = require('wx-server-sdk');
cloud.init({ env: cloud.DYNAMIC_CURRENT_ENV });

// ============================
// 配置你的订阅消息模板ID
const TEMPLATE_IDS = {
  matchNotice: 'YOUR_TEMPLATE_ID_A',   // 比赛通知（名单确认/候补/场主设定）
  teamSplit:   'YOUR_TEMPLATE_ID_C',   // 分队结果通知
};
// ============================

exports.main = async (event, context) => {
  const { type, matchId, playerIds } = event;
  
  try {
    const db = cloud.database();
    const { data: match } = await db.collection('matches').doc(matchId).get();
    
    // 通用发送函数
    const sendTo = async (pid, templateId, messageData) => {
      const { data: p } = await db.collection('players').doc(pid).get();
      if (!p._openid) return;
      try {
        await cloud.openapi.subscribeMessage.send({
          touser: p._openid,
          templateId: templateId,
          page: `pages/match/detail?id=${matchId}`,
          data: messageData
        });
      } catch (e) { console.error('发送失败', e.message); }
    };
    
    // 1. 名单确认通知
    if (type === 'confirmed') {
      const confirmedIds = match.registrations
        .filter(r => r.status === 'confirmed')
        .map(r => r.playerId);
      for (const pid of confirmedIds) {
        await sendTo(pid, TEMPLATE_IDS.matchNotice, {
          thing1: { value: match.title },
          time2: { value: match.date + ' ' + match.time },
          thing3: { value: '您已成功报名，请准时参加！' }
        });
      }
      return { success: true, sent: confirmedIds.length };
    }
    
    // 2. 候补晋升通知
    if (type === 'promoted') {
      for (const pid of (playerIds || [])) {
        await sendTo(pid, TEMPLATE_IDS.matchNotice, {
          thing1: { value: match.title },
          time2: { value: match.date + ' ' + match.time },
          thing3: { value: '恭喜您从候补进入正式名单！' }
        });
      }
      return { success: true };
    }
    
    // 3. 场主/护法设定通知
    if (type === 'owner_set') {
      const notifyIds = playerIds || [match.ownerId, match.assistantId].filter(Boolean);
      for (const pid of notifyIds) {
        const role = pid === match.ownerId ? '场主' : '护法';
        await sendTo(pid, TEMPLATE_IDS.matchNotice, {
          thing1: { value: match.title },
          time2: { value: match.date + ' ' + match.time },
          thing3: { value: `您已被设定为${role}，请提前签场！` }
        });
      }
      return { success: true };
    }
    
    // 4. 分队结果通知
    if (type === 'team_split') {
      const teamAPlayers = match.teamA?.players || [];
      const teamBPlayers = match.teamB?.players || [];
      const allPlayers = [...new Set([...teamAPlayers, ...teamBPlayers])];
      
      for (const pid of allPlayers) {
        const inA = teamAPlayers.includes(pid);
        const teamName = inA ? match.teamA.name : match.teamB.name;
        const teamColor = inA ? match.teamA.color : match.teamB.color;
        const captain = inA ? match.teamA.captainId : match.teamB.captainId;
        const { data: captainPlayer } = await db.collection('players').doc(captain).get().catch(() => ({}));
        
        await sendTo(pid, TEMPLATE_IDS.teamSplit, {
          thing1: { value: match.title },
          thing2: { value: `${teamName}${teamColor ? '（' + teamColor + '）' : ''}` },
          thing3: { value: `队长：${captainPlayer?.nickname || '待定'}` }
        });
      }
      return { success: true, sent: allPlayers.length };
    }
    
    return { success: false, error: '未知通知类型' };
  } catch (e) {
    return { success: false, error: e.message };
  }
};
```

#### `cloudfunctions/sendReminders/index.js`

```javascript
// ============================
// 配置你的提醒模板ID
const REMINDER_TEMPLATE_ID = 'YOUR_TEMPLATE_ID_B'; // 赛前提醒
// ============================
```

将文件中的 `const templateId = '';` 替换为实际ID。

---

### 2. 客户端授权代码

用户首次触发通知前，必须调用 `wx.requestSubscribeMessage` 授权。以下场景需要授权：

#### 场景A：用户点击"报名"时

在 `pages/match/detail.vue` 的 `showRegister` 方法开头添加：

```javascript
async showRegister() {
  // 请求订阅消息授权（名单确认通知 + 候补晋升通知）
  try {
    await wx.requestSubscribeMessage({
      tmplIds: [
        'YOUR_TEMPLATE_ID_A', // matchNotice
      ]
    });
  } catch (e) {
    // 用户取消或拒绝，静默处理
  }
  
  // ... 原有报名逻辑
}
```

#### 场景B：管理员设定场主/护法时

在 `pages/match/detail.vue` 的 `setOwner` / `setAssistant` 方法中添加：

```javascript
async setOwner(playerId) {
  try {
    await wx.requestSubscribeMessage({
      tmplIds: ['YOUR_TEMPLATE_ID_A']
    });
  } catch (e) {}
  
  // ... 原有设定逻辑
}
```

#### 场景C：分队完成后发送通知

在 `pages/match/teamSplit.vue` 的 `save` 方法中添加：

```javascript
async save() {
  // 保存分队
  // ... 原有保存逻辑
  
  // 请求授权并发送分队通知
  try {
    await wx.requestSubscribeMessage({
      tmplIds: ['YOUR_TEMPLATE_ID_C'] // teamSplit
    });
    
    // 发送分队通知
    await wx.cloud.callFunction({
      name: 'sendNotification',
      data: { type: 'team_split', matchId: this.matchId }
    });
  } catch (e) {}
  
  uni.showToast({ title: '分队已保存' });
  setTimeout(() => uni.navigateBack(), 500);
}
```

---

## 五、触发通知调用点

确保以下业务逻辑中调用了 `sendNotification` 云函数：

| 业务动作 | 调用代码 | 位置 |
|----------|----------|------|
| 管理员确认名单 | `sendNotification({ type: 'confirmed', matchId })` | `detail.vue` → `confirmTeam` |
| 候补晋升 | `sendNotification({ type: 'promoted', matchId, playerIds })` | `processRegistration` 云函数 |
| 取消报名后候补晋升 | `sendNotification({ type: 'promoted', matchId, playerIds: [promotedId] })` | `cancelRegistration` 云函数 |
| 设定场主/护法 | `sendNotification({ type: 'owner_set', matchId, playerIds })` | `detail.vue` → `setOwner`/`setAssistant` |
| 分队完成 | `sendNotification({ type: 'team_split', matchId })` | `teamSplit.vue` → `save` |
| 赛前24h/3h提醒 | `sendReminders` 定时云函数自动触发 | 定时触发器 |

---

## 六、定时触发器配置

### 方式一：本地 config.json（推荐）

在 `cloudfunctions/sendReminders/` 下创建 `config.json`：

```json
{
  "triggers": [
    {
      "name": "match-reminder",
      "type": "timer",
      "config": "0 0 * * * * *"
    }
  ]
}
```

右键 `sendReminders` → **"创建并部署：云端安装依赖"**，触发器自动创建。

### 方式二：腾讯云页面配置

进入 `sendReminders` 云函数 → 触发管理 → 创建触发器：

| 配置项 | 值 |
|--------|------|
| 触发方式 | 定时触发 |
| 触发周期 | 自定义 |
| Cron 表达式 | `0 0 * * * * *`（每小时整点） |

---

## 七、字段长度限制（重要）

发送消息时，字段值不能超过以下长度，否则发送失败：

| 字段类型 | 最大长度 | 示例 |
|----------|----------|------|
| thing（事物） | 20个字符 | 周中野球局 |
| time（时间） | 17个字符 | 2025-06-28 20:00 |
| date（日期） | 17个字符 | 2025年06月28日 |
| phrase（短语） | 20个字符 | 已成功报名 |
| character_string（字符串） | 32个字符 | A队（红色） |

**超过长度会直接导致发送失败，发送前务必截断！**

---

## 八、常见问题

### Q1：用户点了"拒绝"，还能再发吗？

不能。用户拒绝后，后续调用 `wx.requestSubscribeMessage` 不会弹窗。需要引导用户：
- 点击右上角 "..." → 设置 → 订阅消息 → 手动打开。

### Q2：模板ID填错了会怎样？

云函数调用 `subscribeMessage.send` 会返回错误：
- `43101`：用户未授权
- `40037`：模板ID不正确
- `47001`：字段格式错误

### Q3：为什么用户没收到消息？

排查顺序：
1. 用户是否授权了该模板？（`wx.requestSubscribeMessage` 返回 `accept`）
2. 模板ID是否填写正确？
3. 字段名是否和模板配置一致？（如 `thing1` 不是 `thing1.DATA`）
4. 字段值是否超长？
5. 云函数日志中是否有错误？

### Q4：赛前提醒的定时触发器没有执行？

检查：
1. `sendReminders` 是否已部署？
2. 触发器是否在腾讯云控制台显示为"已启用"？
3. 云函数日志中是否有定时触发记录？

---

## 九、部署检查清单

- [ ] 微信公众平台已开通订阅消息
- [ ] 已添加所需模板（matchNotice / teamSplit / reminder）
- [ ] 已复制模板ID到 `sendNotification/index.js`
- [ ] 已复制模板ID到 `sendReminders/index.js`
- [ ] 客户端已添加 `wx.requestSubscribeMessage` 授权代码
- [ ] `sendReminders` 已部署且定时触发器已配置
- [ ] 所有调用点已添加 `sendNotification` 云函数调用
