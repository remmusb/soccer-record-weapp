# 部署与测试指南：场主/护法修复 + 康体通显示

## 本次修复的两个问题

1. **更换场主和添加护法无反应** — `showActionSheet` 缺少 `fail` 回调，且超过6个选项会直接静默失败；同时客户端直接写数据库可能因权限被拒绝
2. **报名接龙显示球员康体通后四位** — 在昵称旁显示 `(后四位)`，方便管理员识别

---

## 一、部署步骤

### 步骤 1：部署 `updateMatch` 云函数（新创建）

在微信开发者工具中：
1. 展开 **cloudfunctions/updateMatch/**
2. 确认文件夹包含：
   - `index.js` — 通用更新比赛信息
   - `package.json` — 依赖配置
3. 右键 `updateMatch` 文件夹 → **创建并部署：云端安装依赖**
4. 等待部署完成

> 这是新创建的云函数，用于替代客户端直接写数据库操作。

### 步骤 2：重新编译上传客户端代码

在 HBuilderX 中：
1. 找到以下文件，确保已保存：
   - `pages/match/detail.vue`（场主/护法修复 + 康体通显示 + 24小时退出）
2. 上传流程：
   - 工具栏点击 **运行 → 运行到小程序模拟器 → 微信开发者工具**
   - 在微信开发者工具中：点击 **上传** → 填写版本号（如 `1.2.5`）→ 确定

### 步骤 3：提交审核（正式版）

1. 登录[微信公众平台](https://mp.weixin.qq.com)
2. 进入 **版本管理**
3. 找到上传的版本 → **提交审核**
4. 审核通过后 → **发布**

---

## 二、测试验证

### 测试 1：更换场主（detail.vue）

**场景 A：报名人数 ≤ 6 人**

1. 管理员进入比赛详情（管理员操作区可见）
2. 确保已报名人数 ≤ 6 人
3. 点击场主旁边的 **"更换"** 或 **"设定"**
4. 验证：弹出 `showActionSheet`，显示所有报名人员的昵称
5. 选择一个人
6. 验证：成功设定为场主，页面刷新后显示新场主

**场景 B：报名人数 > 6 人**

1. 管理员进入比赛详情
2. 确保已报名人数 > 6 人
3. 点击场主旁边的 **"更换"**
4. 验证：弹出 picker 选择器弹窗（而非 actionSheet），显示所有人员
5. 滚动选择一个人 → 点击确认
6. 验证：成功设定为场主

**场景 C：无报名人员**

1. 管理员进入一个无人报名的比赛
2. 点击 **"设定场主"**
3. 验证：显示 toast "暂无报名人员"

---

### 测试 2：添加护法（detail.vue）

**场景 A：正常添加**

1. 管理员进入比赛详情
2. 点击护法区域的 **"添加"**
3. 验证：弹出选择列表，显示可添加的队员（排除已有护法和场主）
4. 选择一个人
5. 验证：成功添加为护法，页面刷新后显示

**场景 B：护法已达4人上限**

1. 管理员添加4个护法
2. 再次点击 **"添加"**
3. 验证：显示 toast "护法最多4人"

**场景 C：移除护法**

1. 管理员点击已有护法旁边的 **"移除"**
2. 验证：确认弹窗 → 点击确认后移除成功

---

### 测试 3：康体通后四位显示（detail.vue）

1. 确保某些球员已填写康体通后四位（在注册/编辑页面）
2. 进入任意比赛详情 → 查看报名接龙列表
3. 验证：已填写后四位的球员，昵称旁显示 `(后四位)`，例如：
   - `威哥 (1234)` 👑
   - `张三 (5678)`
4. 未填写后四位的球员不显示括号

---

## 三、修改内容汇总

### `pages/match/detail.vue`

| 修改项 | 说明 |
|--------|------|
| `changeOwner` | 添加 `fail` 回调；超过6项时使用 picker 弹窗；改用 `updateMatch` 云函数 |
| `addAssistant` | 同上 |
| `removeAssistant` | 改用 `updateMatch` 云函数 |
| `doChangeOwner` | 提取为独立方法，供 picker 和 actionSheet 共用 |
| `doAddAssistant` | 提取为独立方法 |
| `loadMatch` | `assistantIds` 初始化改用 `updateMatch` 云函数 |
| 报名列表 | 昵称旁添加 `kttLast4` 显示（`(后四位)`） |
| 样式 | 新增 `.ktt-tag` 样式 |
| 模板 | 新增角色选择器 picker 弹窗（超过6人用） |
| data | 新增 `showRolePicker`、`rolePickerItems` 等字段 |

### 新创建：`cloudfunctions/updateMatch/index.js`

通用更新比赛信息云函数，接收 `matchId` 和 `updateData`，使用管理员权限更新数据库，不受客户端权限限制。

### 新创建：`cloudfunctions/updateMatch/package.json`

依赖 `wx-server-sdk`。

---

## 四、常见问题 FAQ

**Q1：为什么点击"更换"没有任何反应？**
> 已修复。之前有两个问题：
> 1. `showActionSheet` 没有 `fail` 回调，失败后静默无声
> 2. 报名人数超过 6 人时，`showActionSheet` 会直接失败（小程序限制最多6项）
> 
> 现在已添加 `fail` 回调，并且超过6人时自动切换到 picker 弹窗。

**Q2：为什么设置场主后页面没有刷新？**
> 检查 `updateMatch` 云函数是否已部署。如果未部署，客户端数据库写入会因权限失败。

**Q3：康体通后四位为什么不显示？**
> 球员需要在注册/编辑页面填写康体通后四位。如果未填写，不会显示括号。

**Q4：管理员操作区为什么不显示？**
> 管理员身份通过 `login` 云函数判定。检查该用户是否在 `admins` 集合中。

---

## 五、回滚方案

如果部署后出现问题：

1. 在 HBuilderX 中右键修改的文件 → **本地历史** → 回滚到修改前的版本
2. 重新编译上传
3. 云函数回滚：在微信开发者工具中删除 `updateMatch` 云函数，恢复旧代码

---

*生成时间：2026-07-02*
