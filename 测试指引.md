# 野球记录小程序 - 完整测试指引

## 一、部署前检查清单

### 1. 云函数部署（8个）

在微信开发者工具中，对每个云函数右键 → **"创建并部署：云端安装依赖"**：

| 云函数 | 状态 | 说明 |
|--------|------|------|
| `login` | □ | 返回用户身份（openid/playerId/isAdmin） |
| `createPlayer` | □ | 管理员创建球员（含birthDate） |
| `getPlayers` | □ | 获取所有球员（绕过权限） |
| `getAdmins` | □ | 获取所有管理员 |
| `addAdmin` | □ | 添加管理员 |
| `removeAdmin` | □ | 移除管理员 |
| `processRegistration` | □ | 确认比赛名单，排序+晋升候补 |
| `cancelRegistration` | □ | 取消报名，检查24h截止+候补晋升 |
| `sendNotification` | □ | **订阅消息发送（需填模板ID）** |
| `sendReminders` | □ | **定时赛前提醒（需配置触发器）** |
| `recalculateStats` | □ | 赛后重新计算球员统计 |
| `exportData` | □ | 导出数据 |

> ⚠️ `sendNotification` 和 `sendReminders` 必须先填入模板ID再部署！

### 2. 模板ID配置

打开 `cloudfunctions/sendNotification/index.js`，替换：

```javascript
const TEMPLATE_IDS = {
  matchNotice: 'YOUR_TEMPLATE_ID_A', // ← 替换为你的模板ID
  teamSplit:   'YOUR_TEMPLATE_ID_C', // ← 替换为你的模板ID
};
```

### 3. 客户端授权代码

以下文件已添加 `wx.requestSubscribeMessage` 授权，但模板ID需替换：

| 文件 | 位置 | 模板ID变量 |
|------|------|------------|
| `pages/match/detail.vue` | `showRegister` 方法 | `YOUR_TEMPLATE_ID_A` |
| `pages/match/detail.vue` | `setOwner`/`setAssistant` 方法 | `YOUR_TEMPLATE_ID_A` |
| `pages/match/teamSplit.vue` | `save` 方法 | `YOUR_TEMPLATE_ID_C` |

将代码中的 `'YOUR_TEMPLATE_ID_A'` 和 `'YOUR_TEMPLATE_ID_C'` 替换为真实模板ID。

### 4. 定时触发器配置

**方式A：本地 config.json（推荐）**

确认 `cloudfunctions/sendReminders/config.json` 存在：

```json
{
  "triggers": [
    {
      "name": "match-reminder",
      "type": "timer",
      "config": "0 0 * * * * *"
    }
  ]
}
```

**方式B：腾讯云页面**

进入 `sendReminders` 云函数 → 触发管理 → 创建触发器：
- 触发方式：定时触发
- 触发周期：自定义
- Cron 表达式：`0 0 * * * * *`（每小时整点）

---

## 二、测试场景（按顺序执行）

### 场景1：管理员创建比赛（需抽场）

**步骤**：
1. 管理员进入小程序 → 点击"创建新场次"
2. 填写：
   - 名称：测试比赛
   - 日期：选择**明天**（方便测试）
   - 开始时间：20:00
   - 结束时间：22:00
   - 地点：测试球场
   - 人数上限：4（方便测试WL）
   - 是否需要抽场：**是**（默认）
   - 备注：测试用
3. 点击"创建场次"

**验证点**：
- □ 创建成功，跳转回首页
- □ 首页显示新场次，状态为"报名中"
- □ 截图截止时间显示为比赛日前13天24点
- □ 数据库 `matches` 集合中该记录 `needScreenshot: true`

---

### 场景2：队员报名（需抽场）

**步骤**：
1. 队员A（已创建球员资料）进入比赛详情
2. 点击"+ 报名"
3. 弹出授权弹窗 → 点击"允许"（订阅消息授权）
4. 报名成功，提示"请上传截图"

**步骤**：
5. 队员B、C、D、E 依次报名（5人报名，上限4人）

**验证点**：
- □ 队员A报名后，状态为"待截图"
- □ 队员A收到上传截图提示
- □ 队员B~E报名后，显示在报名列表中
- □ 所有队员都在报名列表中，暂无WL状态（名单未确认）
- □ 数据库 `registrations` 中所有记录 `status: 'pending_screenshot'`

---

### 场景3：上传抽场截图

**步骤**：
1. 队员A在报名列表中点击"上传截图"
2. 选择图片 → 上传成功

**步骤**：
3. 队员B也上传截图
4. 队员C、D、E **不上传**截图

**验证点**：
- □ 队员A、B显示"📷 截图已上传"，状态变为"已确认"
- □ 队员C、D、E显示"待截图"
- □ 数据库中 `screenshot` 字段有值，`status: 'confirmed'`（A、B）
- □ 数据库中 `status: 'pending_screenshot'`（C、D、E）

---

### 场景4：管理员确认名单（截图截止后）

**步骤**：
1. 管理员进入比赛详情
2. 等待截图截止时间过后（或手动修改数据库提前测试）
3. 管理员操作区显示：
   - "设定场主/护法" 按钮
   - "确认比赛名单" 按钮
4. 点击"确认比赛名单"
5. 确认弹窗 → 点击"确定"

**验证点**：
- □ 名单按优先级排序：A、B（有截图）→ C、D（无截图但先报名）→ E（WL）
- □ A、B、C、D 状态为"已确认"（前4名）
- □ E 状态为"WL"（候补）
- □ 数据库 `teamConfirmed: true`
- □ 已确认队员收到微信服务通知（名单确认通知）
- □ 检查云函数日志，`sendNotification` 成功发送记录

---

### 场景5：候补晋升（队员退出）

**步骤**：
1. 已确认队员C点击"退出"
2. 确认退出弹窗 → 点击"确定"

**验证点**：
- □ 队员C从名单中移除
- □ 候补队员E自动晋升为"已确认"
- □ 队员E收到微信服务通知（候补晋升通知）
- □ 检查云函数日志，`cancelRegistration` 和 `sendNotification` 执行记录

---

### 场景6：管理员设定场主/护法

**步骤**：
1. 管理员进入比赛详情（截图截止后）
2. 点击"设定场主/护法"
3. 选择队员A为场主
4. 选择队员B为护法

**验证点**：
- □ 场主显示"👑 场主：队员A"
- □ 护法显示"🛡️ 护法：队员B"
- □ 队员A、B收到微信服务通知（场主/护法设定通知）
- □ 检查云函数日志

---

### 场景7：管理员分队

**步骤**：
1. 管理员点击"⚡ 自动分队"
2. 页面跳转到分队页
3. 输入队服颜色：A队红色，B队蓝色
4. 选择队长：A队队长A，B队队长B
5. 点击"保存分队"
6. 弹出授权弹窗 → 点击"允许"（分队通知授权）

**验证点**：
- □ 分队保存成功，返回详情页
- □ A队/B队显示队服颜色
- □ 所有参赛队员收到微信服务通知（分队结果通知）
- □ 通知内容包含：队名、队服颜色、队长
- □ 检查云函数日志，`sendNotification` type='team_split' 记录

---

### 场景8：赛前提醒（定时触发器）

**步骤**（手动触发测试）：
1. 在微信开发者工具 → 云开发 → 云函数 → `sendReminders`
2. 点击"测试" → 输入 `{}` → 点击"运行"

**验证点**：
- □ 如果有比赛在24小时内开始，场主/护法收到提醒
- □ 如果有比赛在3小时内开始，场主/护法收到提醒
- □ 检查云函数日志，显示发送成功/失败记录
- □ 数据库中该比赛 `reminder24hSent: true` 或 `reminder3hSent: true`

**真实定时测试**：
- 创建一个比赛，时间设为当前时间 + 25小时
- 等待1小时后，定时触发器执行
- 检查场主/护法是否收到24小时提醒

---

### 场景9：赛后评分

**步骤**：
1. 管理员点击"🏁 结束比赛"
2. 比赛状态变为 completed
3. 等待比赛结束时间过后（或修改数据库测试）

**验证点**：
- □ 评分窗口开启，显示"⭐ 赛后评分"按钮
4. 队员点击"赛后评分"
5. 给队友打分 → 提交

**验证点**：
- □ 评分成功提交
- □ 云函数 `recalculateStats` 重新计算统计
- □ 球员详情页显示新评分

---

### 场景10：取消报名（24小时截止前）

**步骤**：
1. 创建一个比赛，时间为**3天后**（确保在24h截止前）
2. 队员F报名
3. 队员F点击"退出"

**验证点**：
- □ 退出成功，提示"已退出"
- □ 队员F从名单中移除
- □ 如果有候补队员，自动晋升并收到通知

---

### 场景11：取消报名（超过24小时截止）

**步骤**：
1. 创建一个比赛，时间为**当前时间 + 12小时**（已超24h截止）
2. 队员G报名
3. 队员G点击"退出"

**验证点**：
- □ 弹出提示"您已超过取消报名截止时间，需缴纳罚款"
- □ 队员G无法直接退出

---

## 三、检查数据库记录

测试过程中，打开云开发控制台 → 数据库，检查以下集合：

### `matches` 集合

```json
{
  "title": "测试比赛",
  "date": "2025-06-28",
  "time": "20:00",
  "endTime": "22:00",
  "needScreenshot": true,
  "screenshotDeadline": "2025-06-15T00:00",
  "maxPlayers": 4,
  "status": "upcoming",
  "teamConfirmed": true,
  "registrations": [
    { "playerId": "A", "status": "confirmed", "screenshot": "cloud://..." },
    { "playerId": "B", "status": "confirmed", "screenshot": "cloud://..." },
    { "playerId": "C", "status": "confirmed" },
    { "playerId": "D", "status": "confirmed" },
    { "playerId": "E", "status": "WL" }
  ],
  "ownerId": "A",
  "assistantId": "B",
  "teamA": {
    "players": ["A", "C"],
    "score": 0,
    "color": "红色",
    "captainId": "A",
    "name": "A队"
  },
  "teamB": {
    "players": ["B", "D"],
    "score": 0,
    "color": "蓝色",
    "captainId": "B",
    "name": "B队"
  }
}
```

### `players` 集合

```json
{
  "nickname": "张三",
  "name": "张",
  "birthDate": "1990-06",
  "positions": ["ST", "CF"],
  "ratings": {
    "initialRating": 5,
    "peerRatings": [...],
    "adminRatings": [...]
  }
}
```

---

## 四、常见问题排查

### 问题1：报名显示"所有球员已报名"

**排查**：
1. 检查 `detail.vue` 的 `loadMatch` 中 `allPlayers` 是否正确加载
2. 检查数据库 `players` 集合是否有数据
3. 检查数据库权限，是否用 `getPlayers` 云函数获取

### 问题2：管理员操作区不显示

**排查**：
1. 检查 `isAdmin` 是否正确（通过 `login` 云函数返回）
2. 检查 `isScreenshotDeadlinePassed` 是否返回 true
3. 检查 `match.status` 是否为 'upcoming'
4. 检查 `match.teamConfirmed` 是否为 false（确认后不再显示）

### 问题3：名单确认后没有排序

**排查**：
1. 检查 `processRegistration` 云函数是否执行成功
2. 检查云函数日志中的排序逻辑
3. 检查数据库 `registrations` 的 `status` 字段是否更新

### 问题4：没有收到微信通知

**排查步骤**：
1. 检查模板ID是否已填入代码（`'YOUR_TEMPLATE_ID_A'` 必须替换为真实ID）
2. 检查用户是否授权了订阅消息（首次弹窗是否点击"允许"）
3. 检查云函数日志，`sendNotification` 是否有错误
4. 检查错误码：
   - `43101`：用户未授权 → 需要重新引导授权
   - `40037`：模板ID错误 → 检查ID是否正确
   - `47001`：字段格式错误 → 检查字段名和长度
5. 检查字段是否超过20字符限制

### 问题5：定时触发器没有执行

**排查**：
1. 检查 `sendReminders` 是否已部署
2. 检查腾讯云控制台 → 触发器是否显示为"已启用"
3. 检查云函数日志是否有定时触发记录
4. 手动测试：在云函数页面点击"测试"，输入 `{}` 运行

### 问题6：分队不显示年龄

**排查**：
1. 检查球员 `birthDate` 字段是否存在
2. 检查 `teamSplit.vue` 的 `calcAge` 方法
3. 检查数据库中 `birthDate` 格式为 `YYYY-MM`（如 `1990-06`）

---

## 五、快速测试命令

### 手动测试云函数

在微信开发者工具 → 云开发 → 云函数 → 选择函数 → 测试：

**测试 `sendNotification`**：
```json
{
  "type": "confirmed",
  "matchId": "你的比赛ID"
}
```

**测试 `processRegistration`**：
```json
{
  "matchId": "你的比赛ID"
}
```

**测试 `cancelRegistration`**：
```json
{
  "matchId": "你的比赛ID",
  "playerId": "要取消的球员ID"
}
```

**测试 `sendReminders`**：
```json
{}
```

---

## 六、部署后最终检查清单

- [ ] 所有12个云函数已部署
- [ ] `sendNotification` 中模板ID已替换为真实ID
- [ ] `sendReminders` 中模板ID已替换为真实ID
- [ ] 客户端代码中模板ID已替换为真实ID
- [ ] `sendReminders` 定时触发器已配置（每小时执行）
- [ ] 数据库 `matches` 集合已创建
- [ ] 数据库 `players` 集合已创建
- [ ] 数据库 `admins` 集合已创建
- [ ] 至少创建了1个管理员（通过设置页）
- [ ] 至少创建了2个球员（通过球员页）
- [ ] 编译成功，无报错
- [ ] 测试场景1-11全部通过

---

测试完成！如果所有场景都通过，说明功能完整可用。
